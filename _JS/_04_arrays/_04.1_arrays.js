// Ejercicio 1
const toArray = (...data) => data;
console.log(toArray(1, 2, 3, 4));

// Ejercicio 2
const ZerotoArray = (n) => {
  let data = [];
  for (let i = 0; i <= n; i++) {
    data.push(i);
  }
  return data;
};
console.log(ZerotoArray(4));

// Ejercicio 3 - Por acabar

const SmallerNum = (data) => {
    let smaller;
    for (const item of data) {
    }
    return smaller;
  };
  console.log(SmallerNum([50, 2, 30, 90, 5]));
  