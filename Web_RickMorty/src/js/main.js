import { drawCards } from "./dom";
import {
  getCharactersPage1,
  getCharactersPage2,
  getCharactersPage3,
} from "./querysAPI";

// let data = [];

// export const main = () => {
//     getCharacters();
    
//     // data.map(x => console.log(x));
//     // console.log(data.map(x=> x))
// }

// // Metemos los datos
// export const getCharacters = () => {
//     let data = []
//   getCharactersPage1().then((item) => addItemsArray(item));
//   getCharactersPage2().then((item) => addItemsArray(item));
//   getCharactersPage3().then((item) => addItemsArray(item));
// };

// export const addItemsArray = (arr) => arr.map((x) => data.push(x));

// data.map(item => console.log(item))